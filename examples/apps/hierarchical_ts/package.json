{
  "name": "example",
  "version": "0.0.0",
  "description": "",
  "repository": "",
  "author": "Michael Jae-Yoon Chung",
  "license": "MIT",
  "dependencies": {
    "@cycle-robot-drivers/action": "0.0.11",
    "@cycle-robot-drivers/run": "0.0.22",
    "@cycle/state": "^1.1.0",
    "xstream": "11.7.0"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "concurrently": "^4.0.1",
    "live-server": "^1.2.0",
    "mkdirp": "~0.5.0",
    "tsify": "^4.0.0",
    "watchify": "^3.11.0"
  },
  "scripts": {
    "build": "mkdirp dist && browserify index.ts -p tsify --outfile dist/index.js -dv",
    "watch": "mkdirp dist && watchify index.ts -p tsify --outfile dist/index.js -dv",
    "start": "concurrently \"npm run watch\" \"live-server\""
  }
}
